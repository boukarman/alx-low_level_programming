#include <stdio.h>
/**
 * main -Entry point 
 *
 * Description : prints "Programming is like builduing a multilingual puzzle"
 *
 * Return: Always 0 (Success)
 */
int main (void)
{
	puts("\"programming is like builduing a multilingual puzzle");
	return(0);
}
